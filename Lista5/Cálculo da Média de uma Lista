def calcular_media(lista):
    return sum(lista) / len(lista)

# Programa principal
entrada = input("Digite uma lista de números separados por espaço: ")
lista = [float(numero) for numero in entrada.split()]
print(f"A média dos números é: {calcular_media(lista)}")
