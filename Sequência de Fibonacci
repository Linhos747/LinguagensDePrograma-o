# Solicita o número de termos da sequência
N = int(input("Digite o número de termos da Sequência de Fibonacci: "))

# Inicializa os dois primeiros termos
a, b = 0, 1

# Exibe os N primeiros termos da Sequência de Fibonacci
for _ in range(N):
    print(a, end=" ")  # Exibe o termo atual
    a, b = b, a + b  # Atualiza os termos para o próximo passo
