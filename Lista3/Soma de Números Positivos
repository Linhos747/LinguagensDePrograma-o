# Inicializa a soma
soma = 0

# Loop infinito até que um número negativo seja digitado
while True:
    numero = float(input("Digite um número (negativo para parar): "))
    if numero < 0:
        break  # Sai do loop se o número for negativo
    soma += numero  # Adiciona o número à soma

# Exibe a soma dos números positivos
print(f"A soma dos números positivos digitados é: {soma}")
